export default function Home() {
  return (
    <main className="page-shell">
      <div className="card">
        <h1>Image AI backend is running</h1>
        <p>POST a fridge photo data URI to <code>/api/identify-items</code>.</p>
      </div>
    </main>
  );
}
